module.exports = {
  // Add semicolons at the end of statements
  semi: true,
  // Use single quotes instead of double quotes
  singleQuote: true,
  // Set indentation to 2 spaces
  tabWidth: 2,
  // Use spaces for indentation (not tabs)
  useTabs: false,
  // Wrap lines at 80 characters
  printWidth: 80,
  // Add trailing commas wherever possible (arrays, objects, etc.)
  trailingComma: 'all',
  // Add spaces between brackets in object literals
  bracketSpacing: true,
  // Always include parentheses around arrow function parameters
  arrowParens: 'always',
  // Use Unix-style line endings (LF)
  endOfLine: 'lf',
  // Only add quotes around object properties when needed
  quoteProps: 'as-needed',
  // Use double quotes in JSX attributes
  jsxSingleQuote: false,
  // Put the closing bracket on a new line in JSX
  bracketSameLine: false,
  // Don't wrap markdown prose
  proseWrap: 'preserve',
  // Respect CSS display property for whitespace in HTML
  htmlWhitespaceSensitivity: 'css',
  // Auto-format embedded code (like CSS in JS)
  embeddedLanguageFormatting: 'auto',
};
