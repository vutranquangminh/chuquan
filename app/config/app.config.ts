/**
 * App-wide configuration constants. Convention: constants in
 * SCREAMING_SNAKE_CASE. opilot-pm also keeps aic-kits CRUD `TableConfig`
 * objects in this folder (e.g. `<feature>-tables.ts`).
 */
export const APP_NAME = 'chuquan';

export const DEFAULT_PAGE_SIZE = 20;

export const DATE_FORMAT = 'YYYY-MM-DD';
